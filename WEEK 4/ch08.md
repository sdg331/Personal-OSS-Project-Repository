# 로그 이력과 과거 여행 (Git Log History & Time Travel)

## 📋 학습 개요

### 학습 내용
- Git 설정과 저장소 생성
- `init`, `add`, `commit`, `status` 복습
- `log`, `show` 명령어 심화
- 버전 이력 표현법
  - `HEAD`, `HEAD~`, `HEAD~~`, `HEAD~2`
  - `HEAD`, `HEAD^`, `HEAD^1`, `HEAD^^`
  - `HEAD`, `HEAD~`, `HEAD^~`
- 과거 이전 버전으로 이동
  - `checkout HEAD~`

### 학습 목표
- 초기에 Git을 설정하고 저장소를 생성할 수 있다
- 파일을 만들어 버전관리를 위한 과정을 수행할 수 있다
- 버전관리 로그 이력을 확인할 수 있다
- **이전 버전으로 이동한 후 다시 최신 버전으로 돌아올 수 있다**

***

## 1️⃣ 실습 환경 구성

### Git 설정과 저장소 생성

#### 전체 실습 순서

| 순서 | 명령어 | 설명 | 비고 |
|------|--------|------|------|
| **1** | `$ git config --global user.name hskang` | 사용자 이름 | - |
| **1** | `$ git config --global user.email hskang@gmail.com` | 전자메일 설정 | - |
| **1** | `$ git config --global core.autocrlf true` | 자동 줄바꿈 | CR (Carriage Return) |
| **1** | `$ git config --global core.safecrlf false` | 안전 줄바꿈 | LF (Line Feed) |
| **1** | `$ git config --global core.editor 'code --wait'` | 기본 편집기 설정 | - |
| **1** | `$ git config --global init.defaultBranch main` | 기본 브랜치 이름 설정 | - |
| **2** | `$ git init basic` | 저장소 basic 생성 | - |
| **2** | `$ cd basic` | 폴더 이동 | - |
| **3** | `$ ls -al` | 파일 확인 | `.git` 폴더 확인 |

### 실습 명령어

```bash
# Step 1: Git 전역 설정
$ git config --global user.name hskang
$ git config --global user.email hskang@gmail.com
$ git config --global core.autocrlf true
$ git config --global core.safecrlf false
$ git config --global core.editor 'code --wait'
$ git config --global init.defaultBranch main

# Step 2: 저장소 생성 및 이동
$ git init basic
$ cd basic

# Step 3: 저장소 확인
$ ls -al
```

***

## 2️⃣ 첫 번째 커밋 (1st Commit)

### 파일 생성과 커밋

#### 작업 흐름

```
Working Directory → Staging Area → Repository
     (작업 폴더)      (스테이지 영역)    (깃 저장소)
```

### 실습 단계

#### Step 1: 파일 생성
```bash
$ echo aaa > hello.txt
$ cat hello.txt
aaa
```

#### Step 2: 상태 확인
```bash
$ git status
Untracked files:
  hello.txt

$ git status -s
?? hello.txt
```

**상태 기호**:
- `??`: Untracked (추적되지 않는 파일)

#### Step 3: 스테이징
```bash
$ git add hello.txt
$ git status
Changes to be committed:
  new file:   hello.txt

$ git status -s
A  hello.txt
```

**상태 기호**:
- `A `: Added (스테이징된 새 파일)

#### Step 4: 커밋
```bash
$ git commit -m "A"
[main (root-commit) dd3f28a] A
 1 file changed, 1 insertion(+)
 create mode 100644 hello.txt
```

#### Step 5: 로그 확인
```bash
$ git status
nothing to commit, working tree clean

$ git log
commit dd3f28a...
Author: hskang <hskang@gmail.com>
Date:   Mon Dec 8 09:00:00 2025 +0900

    A

$ git log --oneline
dd3f28a A
```

### 현재 상태 시각화

```
Working Directory    Staging Area      Repository
  [hello.txt]         [hello.txt]       [hello.txt]
      aaa                 aaa          A ← HEAD (main)
                                          aaa
```

***

## 3️⃣ 두 번째 커밋 (2nd Commit)

### 파일 수정과 커밋

#### Step 1: 파일 수정
```bash
$ echo bbb >> hello.txt
$ cat hello.txt
aaa
bbb
```

#### Step 2: 상태 확인
```bash
$ git status
Changes not staged for commit:
  modified:   hello.txt

$ git status -s
 M hello.txt
```

**상태 기호**:
- ` M`: Modified (Working Directory에서 수정됨)

#### Step 3: 스테이징 및 커밋 (한 번에)
```bash
$ git commit -am "B"
[main 3a2d414] B
 1 file changed, 1 insertion(+)
```

⭐ **참고**: `-a` 옵션은 Tracked 파일을 자동으로 스테이징

#### Step 4: 로그 확인
```bash
$ git log --oneline
3a2d414 B
dd3f28a A
```

### 추가 파일 수정 (3단계)

#### Step 5: 다시 파일 수정
```bash
$ echo ccc >> hello.txt
$ cat hello.txt
aaa
bbb
ccc

$ git status
Changes not staged for commit:
  modified:   hello.txt

$ git status -s
 M hello.txt
```

### 현재 상태 시각화

```
Working Directory    Staging Area      Repository
  [hello.txt]         [hello.txt]
    aaa                   aaa          B ← HEAD (main)
    bbb                   bbb            aaa
    ccc                                  bbb
                                       ↓
                                       A
                                         aaa
```

***

## 4️⃣ 세 번째 커밋 (3rd Commit)

### 커밋 및 로그 이력 확인

#### Step 1: 커밋
```bash
$ git commit -am "C"
[main 07af7f6] C
 1 file changed, 1 insertion(+)
```

#### Step 2: 상태 확인
```bash
$ git status
nothing to commit, working tree clean
```

#### Step 3: 로그 이력 확인

```bash
# 기본 로그
$ git log
commit 07af7f6...
Author: hskang <hskang@gmail.com>
Date:   Mon Dec 8 09:15:00 2025 +0900

    C

commit 3a2d414...
    B

commit dd3f28a...
    A

# 한 줄 로그
$ git log --oneline
07af7f6 C
3a2d414 B
dd3f28a A

# 역순 로그 (오래된 것부터)
$ git log --reverse --oneline
dd3f28a A
3a2d414 B
07af7f6 C

# 최근 2개만
$ git log -2
$ git log -2 --oneline
07af7f6 C
3a2d414 B

# 그래프와 함께
$ git log --graph --oneline
* 07af7f6 C
* 3a2d414 B
* dd3f28a A
```

***

## 5️⃣ git log 옵션 상세

### 주요 옵션

#### 기본 개념
- `git log`는 **기본적으로 가장 최근의 커밋부터 표시**

### 옵션 목록

| 명령어 | 설명 |
|--------|------|
| `$ git log --graph` | 문자 그림으로 로그 이력 그리기 |
| `$ git log --reverse` | 오래된 커밋부터 표시 (⚠️ `--graph`와 함께 사용 불가) |
| `$ git log --all` | 모든 브랜치의 로그 이력 표시 |
| `$ git log -n` | 최근 n개의 로그 이력 표시 |
| `$ git log --oneline` | 한 줄로 간결하게 표시 |
| `$ git log --patch` 또는 `-p` | 변경 내용까지 표시 |

### 조합 사용 예시

```bash
# 가장 유용한 조합
$ git log --oneline --graph --all

# 최근 5개만 그래프로
$ git log --oneline --graph -5

# 변경 내용 포함
$ git log -p
$ git log --patch
```

***

## 6️⃣ git show 명령어

### 기본 사용법

#### 명령어 구조
```bash
$ git show [commit]
```

#### 기능
- **특정 커밋의 상세 정보 표시**
- 기본값: HEAD (현재 커밋)

### 사용 예시

```bash
# 현재 커밋 (HEAD)
$ git show
$ git show HEAD

# 이전 커밋
$ git show HEAD~

# 전전 커밋
$ git show HEAD~2
$ git show HEAD~~

# 간결하게
$ git show --oneline
```

***

## 7️⃣ HEAD와 버전 표현법

### HEAD란?

#### 정의
- **현재 체크아웃된 커밋을 가리키는 포인터**
- 일반적으로 현재 브랜치의 최신 커밋을 가리킴
- `.git/HEAD` 파일에 저장

### 버전 표현 방식

#### 1. 틸드(`~`) 표기법

| 표현 | 의미 | 설명 |
|------|------|------|
| `HEAD` | 현재 커밋 | 최신 버전 |
| `HEAD~` | 한 단계 이전 | 1st parent |
| `HEAD~1` | 한 단계 이전 | `HEAD~`와 동일 |
| `HEAD~~` | 두 단계 이전 | 2nd parent |
| `HEAD~2` | 두 단계 이전 | `HEAD~~`와 동일 |
| `HEAD~3` | 세 단계 이전 | - |

#### 2. 캐럿(`^`) 표기법

| 표현 | 의미 | 설명 |
|------|------|------|
| `HEAD^` | 한 단계 이전 | 1st parent |
| `HEAD^1` | 한 단계 이전 | `HEAD^`와 동일 |
| `HEAD^^` | 두 단계 이전 | `HEAD^`의 부모 |
| `HEAD^2` | 두 번째 부모 | 병합 커밋에서 사용 |

#### 3. 혼합 표기법

| 표현 | 의미 |
|------|------|
| `HEAD~^` | `HEAD~`의 1st parent |
| `HEAD^~` | `HEAD^`의 1st parent |
| `HEAD~2^` | `HEAD~2`의 1st parent |

### 실습 예시

현재 커밋 이력이 다음과 같다고 가정:

```
C ← HEAD (main)
07af7f6
↓
B ← HEAD~ (HEAD^, HEAD~1, HEAD^1)
3a2d414
↓
A ← HEAD~~ (HEAD^^, HEAD~2)
dd3f28a
```

#### 각 표현의 의미

```bash
# 현재 커밋 C
$ git show HEAD
$ git show 07af7f6

# 이전 커밋 B
$ git show HEAD~
$ git show HEAD^
$ git show HEAD~1
$ git show HEAD^1
$ git show 3a2d414

# 전전 커밋 A
$ git show HEAD~~
$ git show HEAD^^
$ git show HEAD~2
$ git show HEAD~^
$ git show HEAD^~
$ git show dd3f28a
```

***

## 8️⃣ 복잡한 상태 실습

### 파일 수정 + 스테이징 + 다시 수정

#### Step 8: 파일 수정
```bash
$ echo ddd >> hello.txt
$ git status
Changes not staged for commit:
  modified:   hello.txt

$ git status -s
 M hello.txt
```

#### Step 9: 스테이징
```bash
$ git add hello.txt
$ git status
Changes to be committed:
  modified:   hello.txt

$ git status -s
M  hello.txt
```

#### Step 10: 다시 수정
```bash
$ echo eee >> hello.txt
$ git status
Changes to be committed:
  modified:   hello.txt

Changes not staged for commit:
  modified:   hello.txt

$ git status -s
MM hello.txt
```

**상태 기호**:
- `MM`: Modified in both (Staging Area와 Working Directory 둘 다 수정됨)

### 현재 상태 시각화

```
Working Directory    Staging Area      Repository
  [hello.txt]         [hello.txt]       [hello.txt]
    aaa                   aaa          C ← HEAD (main)
    bbb                   bbb            aaa
    ccc                   ccc            bbb
    ddd                   ddd            ccc
    eee                                ↓
                                       B ← HEAD~
                                         aaa
                                         bbb
                                       ↓
                                       A ← HEAD~~
                                         aaa
```

***

## 9️⃣ 과거로의 시간 여행 (Time Travel)

### git checkout 명령어

#### 기본 개념
- **현재 브랜치에서 과거 커밋으로 이동**
- 그 상태에서 당시의 파일 내용을 확인 가능
- Working Directory의 파일들이 과거 버전으로 변경됨

### 주요 명령어

| 명령어 | 설명 |
|--------|------|
| `$ git checkout HEAD~` | HEAD 이전 커밋으로 이동 |
| `$ git checkout -` | 이전 checkout으로 이동 |
| `$ git checkout main` | 브랜치의 마지막 커밋으로 이동 |
| `$ git checkout <commit-hash>` | 특정 커밋으로 이동 |

### 과거 커밋으로 이동

#### 사전 조건
- ⚠️ **현재 상태가 깨끗해야 checkout이 가능**
- 수정사항이 있으면 커밋 또는 stash 필요

#### 실습: HEAD~ 로 이동

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$ git checkout HEAD~
Note: switching to 'HEAD~'.

You are in 'detached HEAD' state. You can look around, make experimental
changes and commit them, and you can discard any commits you make in this
state without impacting any branches by switching back to a branch.

If you want to create a new branch to retain commits you create, you may
do so (now or later) by using -c with the switch command. Example:

  git switch -c <new-branch-name>

Or undo this operation with:

  git switch -

Turn off this advice by setting config variable advice.detachedHead to false

HEAD is now at 3a2d414 B

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((3a2d414...))
$
```

***

## 🔟 Detached HEAD 상태

### Detached HEAD란?

#### 정의
- **HEAD가 마지막 커밋이 아닌 그 이전을 가리키는 상태**
- 브랜치가 아닌 특정 커밋을 직접 가리킴
- 프롬프트가 브랜치 이름 대신 커밋 해시를 표시

#### 특징
- 브랜치에서 분리된 상태
- 이 상태에서 커밋하면 브랜치에 포함되지 않음
- 실험적 변경에 유용

### Detached HEAD 확인

#### 프롬프트 변화
```bash
# 정상 상태
(main)

# Detached HEAD 상태
((3a2d414...))
```

#### git status로 확인
```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((3a2d414...))
$ git status
HEAD detached at 3a2d414
nothing to commit, working tree clean
```

#### git log로 확인
```bash
# 기본 로그 (현재 커밋까지만)
$ git log --oneline
3a2d414 (HEAD) B
dd3f28a A

# 모든 브랜치 포함
$ git log --oneline --all
07af7f6 (main) C
3a2d414 (HEAD) B
dd3f28a A
```

**차이점**:
- `git log`: 현재 HEAD부터 과거만 표시
- `git log --all`: 모든 브랜치 포함

#### 파일 내용 확인
```bash
$ cat hello.txt
aaa
bbb
```

과거 커밋 B 시점의 내용만 표시됨 (ccc가 없음)

***

## 1️⃣1️⃣ 최신 버전으로 복귀

### 되돌아오는 방법

#### 방법 1: 브랜치 이름 사용
```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((3a2d414...))
$ git checkout main
Previous HEAD position was 3a2d414 B
Switched to branch 'main'

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$
```

#### 방법 2: 이전 위치로 (`-` 사용)
```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((3a2d414...))
$ git checkout -
Previous HEAD position was 3a2d414 B
Switched to branch 'main'

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$
```

⭐ **추천**: `-` 기호는 "이전 checkout 위치"를 의미

### 복귀 후 확인

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$ git log --oneline
07af7f6 (HEAD -> main) C
3a2d414 B
dd3f28a A

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$ cat hello.txt
aaa
bbb
ccc
```

최신 버전 C로 복귀 완료

***

## 1️⃣2️⃣ 전전 커밋으로 이동 (HEAD~2)

### 더 먼 과거로 이동

#### 실습: 커밋 A로 이동

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$ git log --oneline
07af7f6 (HEAD -> main) C
3a2d414 B
dd3f28a A

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$ git checkout HEAD~2
Note: switching to 'HEAD~2'.

You are in 'detached HEAD' state. ...

HEAD is now at dd3f28a A

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((dd3f28a...))
$
```

#### 상태 확인

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((dd3f28a...))
$ git log --oneline --all
07af7f6 (main) C
3a2d414 B
dd3f28a (HEAD) A

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((dd3f28a...))
$ cat hello.txt
aaa
```

가장 초기 버전 A의 내용만 표시 (bbb, ccc가 없음)

#### 다시 복귀

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic ((dd3f28a...))
$ git checkout main
Previous HEAD position was dd3f28a A
Switched to branch 'main'

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/basic (main)
$ cat hello.txt
aaa
bbb
ccc
```

***

## 1️⃣3️⃣ git checkout vs git switch

### 명령어 비교

Git 2.23 버전부터 `git switch` 명령어 도입

| 설명 | git checkout | git switch |
|------|-------------|------------|
| 과거 커밋으로 이동 | `$ git checkout [이전커밋]` | `$ git switch -d [이전커밋]` |
| 브랜치로 이동 | `$ git checkout [branch]` | `$ git switch [branch]` |
| 새 브랜치 생성 및 이동 | `$ git checkout -b [newBranch]` | `$ git switch -c [newBranch]` |

### git switch 사용 예시

```bash
# 과거 커밋으로 이동 (detach 옵션 필요)
$ git switch -d HEAD~

# 브랜치로 이동
$ git switch main

# 이전 위치로
$ git switch -

# 새 브랜치 생성 및 이동
$ git switch -c feature-branch
```

⭐ **추천**: 최신 Git 버전에서는 `git switch` 사용 권장

***

## 1️⃣4️⃣ 전체 워크플로우 정리

### 시간 여행 전체 과정

```bash
# 1. 현재 위치 확인
$ git log --oneline
07af7f6 (HEAD -> main) C
3a2d414 B
dd3f28a A

# 2. 과거로 이동 (B 커밋)
$ git checkout HEAD~
HEAD is now at 3a2d414 B

# 3. 파일 확인
$ cat hello.txt
aaa
bbb

# 4. 로그 확인 (--all 옵션 중요!)
$ git log --oneline --all
07af7f6 (main) C
3a2d414 (HEAD) B
dd3f28a A

# 5. 더 과거로 이동 (A 커밋)
$ git checkout HEAD~
HEAD is now at dd3f28a A

# 6. 파일 확인
$ cat hello.txt
aaa

# 7. 최신으로 복귀
$ git checkout main
Switched to branch 'main'

# 8. 파일 확인
$ cat hello.txt
aaa
bbb
ccc
```

***

## 📌 핵심 요약 (Summary)

### 저장소 생성
```bash
$ git init basic
```

### Git 저장소에 저장
```bash
$ git add .
$ git commit -m "메시지"
```

### 모든 커밋 이력 보기
```bash
$ git log
$ git log --oneline --graph --all -n
```

**주요 옵션**:
- `--oneline`: 한 줄로 간결하게
- `--graph`: 그래프 형태
- `--all`: 모든 브랜치
- `-n`: 최근 n개

### 특정 커밋 이력 보기
```bash
$ git show [HEAD]
$ git show --oneline
```

### 바로 이전 버전으로 가기
```bash
$ git checkout HEAD~
```

### 다시 최신 버전으로 돌아오기
```bash
$ git checkout main
```

### 다시 checkout 이전으로 돌아오기
```bash
$ git checkout -
```

### HEAD 표현법
- `HEAD`: 현재 커밋
- `HEAD~` = `HEAD^` = `HEAD~1`: 이전 커밋
- `HEAD~~` = `HEAD^^` = `HEAD~2`: 전전 커밋