# [실습] 커밋과 로그 이력, 과거 여행 (Git Commit, Log & Time Travel Practice)

## 📋 학습 개요

### 학습 내용
- Git 설정과 저장소 생성
- 커밋 과정
- 이전 버전으로 이동 후 다시 최신 버전으로 이동

### 학습 목표
- Git 초기 설정하고 저장소를 생성할 수 있다
- 파일을 만들어 버전관리를 위한 과정을 수행할 수 있다
- 버전관리 로그 이력을 확인할 수 있다
- **이전 버전으로 이동하고 다시 최신 버전으로 이동할 수 있다**

***

## 1️⃣ Git 설정과 저장소 생성

### 전역 설정 및 저장소 초기화

#### 실습 단계

| 단계 | 명령어 | 설명 |
|------|--------|------|
| **1** | `$ git config --global user.name hskang` | 사용자 이름 설정 |
| **1** | `$ git config --global user.email hskang@gmail.com` | 전자메일 설정 |
| **1** | `$ git config --global core.autocrlf true` | 자동 줄바꿈 설정 |
| **1** | `$ git config --global core.safecrlf false` | 안전 줄바꿈 설정 |
| **1** | `$ git config --global core.editor 'code --wait'` | 기본 편집기 설정 |
| **1** | `$ git config --global init.defaultBranch main` | 기본 브랜치 이름 설정 |
| **2** | `$ git init basic` | 저장소 생성 |
| **2** | `$ cd basic` | 폴더 이동 |
| **3** | `$ ls -al` | 파일 확인 (`.git` 폴더 확인) |

### 전체 명령어

```bash
# Step 1: Git 전역 설정
$ git config --global user.name hskang
$ git config --global user.email hskang@gmail.com
$ git config --global core.autocrlf true
$ git config --global core.safecrlf false
$ git config --global core.editor 'code --wait'
$ git config --global init.defaultBranch main

# Step 2: 저장소 생성 및 이동
$ git init basic
$ cd basic

# Step 3: 저장소 확인
$ ls -al
```

***

## 2️⃣ 3번의 커밋 후 과거와 현재로의 여행

### 실습 목표 시각화

```
이전 이전 버전          이전 버전             최신 버전
(HEAD~~)              (HEAD~)              (HEAD)
HEAD~2                HEAD~1               main
HEAD^^                HEAD^
HEAD^~                HEAD^1
HEAD~^                

A                     B                    C
hello.txt             hello.txt            hello.txt
aaa                   aaa                  aaa
                      bbb                  bbb
                                           ccc

     ← checkout HEAD~2
          ← checkout HEAD~
                         → checkout main
                         → checkout -
```

### 시간 여행 명령어 미리보기

```bash
# 과거로 이동
$ git checkout HEAD~      # 1단계 이전
$ git checkout HEAD~2     # 2단계 이전

# 현재로 복귀
$ git checkout main       # 브랜치 최신으로
$ git checkout -          # 직전 위치로
```

***

## 3️⃣ 첫 번째 커밋 (1회 커밋)

### 실습 과정

```
Working Directory → Staging Area → Repository
   (작업 디렉토리)     (스테이징 영역)    (깃 저장소)
```

### 단계별 명령어

#### ① 파일 수정 (Working Directory)
```bash
$ echo aaa > hello.txt
$ cat hello.txt
aaa

$ git status
Untracked files:
  hello.txt

$ git status -s
?? hello.txt
```

**상태**: `??` (Untracked - 추적되지 않는 파일)

#### ② 파일 추가 (Staging Area)
```bash
$ git add hello.txt

$ git status
Changes to be committed:
  new file:   hello.txt

$ git status -s
A  hello.txt
```

**상태**: `A ` (Added - 스테이징된 새 파일)

#### ③ 첫 번째 커밋 (Repository)
```bash
$ git commit -m "A"
[main (root-commit) dd3f28a] A
 1 file changed, 1 insertion(+)

$ git status
nothing to commit, working tree clean

$ git log
commit dd3f28a...
Author: hskang <hskang@gmail.com>
Date:   Mon Dec 8 09:00:00 2025 +0900

    A

$ git log --oneline
dd3f28a A

$ git log --patch
$ git log -p
```

### 현재 상태

```
Working Directory    Staging Area     Repository
     aaa                 aaa         A ← HEAD (main)
                                        aaa
```

***

## 4️⃣ 두 번째 커밋 (총 2회 커밋)

### 파일 수정 및 커밋

#### ④ 파일 수정
```bash
$ echo bbb >> hello.txt
$ cat hello.txt
aaa
bbb

$ git status
Changes not staged for commit:
  modified:   hello.txt

$ git status -s
 M hello.txt
```

**상태**: ` M` (Modified - Working Directory에서 수정됨)

#### ⑤ 두 번째 커밋 (스테이징 생략)
```bash
$ git commit -am "B"
[main 3a2d414] B
 1 file changed, 1 insertion(+)

$ git status
nothing to commit, working tree clean

$ git log
commit 3a2d414 B
commit dd3f28a A

$ git log --oneline
3a2d414 B
dd3f28a A

$ git log --patch
```

⭐ **참고**: `-a` 옵션으로 add와 commit을 한 번에 수행

#### ⑥ 다시 파일 수정
```bash
$ echo ccc >> hello.txt
$ cat hello.txt
aaa
bbb
ccc

$ git status
Changes not staged for commit:
  modified:   hello.txt

$ git status -s
 M hello.txt
```

### 현재 상태

```
Working Directory    Staging Area     Repository
     aaa                 aaa         B ← HEAD (main)
     bbb                 bbb            aaa
     ccc                                bbb
                                     ↓
                                     A
                                        aaa
```

***

## 5️⃣ 세 번째 커밋 (총 3회 커밋)

### 커밋 및 로그 이력 확인

#### ⑦ 세 번째 커밋
```bash
$ git commit -am "C"
[main 07af7f6] C
 1 file changed, 1 insertion(+)

$ git status
nothing to commit, working tree clean

$ git log
commit 07af7f6 C
commit 3a2d414 B
commit dd3f28a A

$ git log --oneline
07af7f6 C
3a2d414 B
dd3f28a A

$ git log --patch
```

### 다양한 로그 명령어

```bash
# 역순 로그 (오래된 것부터)
$ git log --reverse --oneline
dd3f28a A
3a2d414 B
07af7f6 C

# 최근 2개만
$ git log -2
$ git log -2 --oneline
07af7f6 C
3a2d414 B

# 그래프와 함께
$ git log --graph --oneline
* 07af7f6 C
* 3a2d414 B
* dd3f28a A
```

### git show 명령어

```bash
# 현재 커밋
$ git show
$ git show --oneline

# 특정 커밋
$ git show HEAD      # 현재 (C)
$ git show HEAD~     # 이전 (B)
$ git show HEAD~2    # 전전 (A)
```

***

## 6️⃣ 복잡한 상태 만들기

### 수정 + 추가 + 다시 수정

#### ⑧ 수정
```bash
$ echo ddd >> hello.txt

$ git status
Changes not staged for commit:
  modified:   hello.txt

$ git status -s
 M hello.txt
```

#### ⑨ 추가 (스테이징)
```bash
$ git add hello.txt

$ git status
Changes to be committed:
  modified:   hello.txt

$ git status -s
M  hello.txt
```

#### ⑩ 다시 수정
```bash
$ echo eee >> hello.txt

$ git status
Changes to be committed:
  modified:   hello.txt

Changes not staged for commit:
  modified:   hello.txt

$ git status -s
MM hello.txt
```

**상태**: `MM` (Staging Area와 Working Directory 둘 다 수정됨)

### 현재 최종 상태

```
Working Directory    Staging Area     Repository
     aaa                 aaa         C ← HEAD (main)
     bbb                 bbb            aaa
     ccc                 ccc            bbb
     ddd                 ddd            ccc
     eee                              ↓
                                      B ← HEAD~
                                         aaa
                                         bbb
                                      ↓
                                      A ← HEAD~~
                                         aaa
```

***

## 7️⃣ 과거 버전으로 이동 준비

### 작업 영역 정리 (Clean 상태 만들기)

#### 문제 상황
- `git checkout` 명령은 **작업 영역이 깨끗한 상태에서만 가능**
- 현재 상태: Working Directory에 수정사항(`eee`) 존재

#### 해결 방법

##### 방법 1: git stash (임시 저장) ⭐ 추천
```bash
$ git checkout HEAD~
error: Your local changes to the following files would be overwritten by checkout:
    hello.txt
Please commit your changes or stash them before you switch branches.

$ git stash
Saved working directory and index state WIP on main: 07af7f6 C

$ cat hello.txt
aaa
bbb
ccc
```

**기능**: 작업 디렉토리와 스테이징 영역을 임시 저장하고 깨끗한 상태로 만듦

##### 방법 2: git restore (복원)
```bash
# 스테이징 취소
$ git restore --staged hello.txt

# 작업 디렉토리 변경사항 취소
$ git restore hello.txt

$ cat hello.txt
aaa
bbb
ccc
```

**차이점**:
- `git stash`: 변경사항을 보관 (나중에 복구 가능)
- `git restore`: 변경사항을 버림 (복구 불가)

### 준비 완료 상태

```
Working Directory    Staging Area     Repository
     aaa                 aaa         C ← HEAD (main)
     bbb                 bbb            aaa
     ccc                 ccc            bbb
                                        ccc
```

⭐ **중요**: 3 영역이 모두 동일한 상태 (Clean)

***

## 8️⃣ 과거로 시간 여행

### 바로 이전 버전(HEAD~)으로 이동

#### ⑪ 과거로 이동
```bash
$ git checkout HEAD~
Note: switching to 'HEAD~'.

You are in 'detached HEAD' state. ...

HEAD is now at 3a2d414 B

PC@DESKTOP MINGW64 /c/[smart Git]/basic ((3a2d414...))
$
```

### 과거 상태 확인

```bash
# 상태 확인
$ git status
HEAD detached at 3a2d414
nothing to commit, working tree clean

# 파일 내용 확인
$ cat hello.txt
aaa
bbb

# 로그 확인 (현재 HEAD까지만)
$ git log
commit 3a2d414 B
commit dd3f28a A

$ git log --oneline
3a2d414 (HEAD) B
dd3f28a A

# 그래프로 확인
$ git log --graph
* 3a2d414 (HEAD) B
* dd3f28a A

# 모든 브랜치 포함
$ git log --all --oneline
07af7f6 (main) C
3a2d414 (HEAD) B
dd3f28a A
```

⭐ **중요**: `--all` 옵션으로 모든 커밋 이력 확인 가능

### 현재 위치 시각화

```
Working Directory    Staging Area     Repository
     aaa                 aaa         C (main)
     bbb                 bbb            aaa
                                        bbb
                                        ccc
                                     ↓
                                     B ← HEAD (detached)
                                        aaa
                                        bbb
                                     ↓
                                     A
                                        aaa
```

***

## 9️⃣ 최신 버전으로 복귀

### 방법 1: 브랜치 이름 사용
```bash
$ git checkout main
Previous HEAD position was 3a2d414 B
Switched to branch 'main'

PC@DESKTOP MINGW64 /c/[smart Git]/basic (main)
$

$ cat hello.txt
aaa
bbb
ccc
```

### 방법 2: 이전 위치로 (`-` 사용) ⭐ 추천
```bash
$ git checkout -
Previous HEAD position was 3a2d414 B
Switched to branch 'main'

$ cat hello.txt
aaa
bbb
ccc
```

**의미**: `-`는 "직전 checkout 위치"를 나타냄 (cd - 와 유사)

***

## 🔟 더 먼 과거로 이동 (HEAD~2)

### 전전 커밋으로 이동

```bash
$ git checkout HEAD~2
Note: switching to 'HEAD~2'.

You are in 'detached HEAD' state. ...

HEAD is now at dd3f28a A

PC@DESKTOP MINGW64 /c/[smart Git]/basic ((dd3f28a...))
$
```

### 확인

```bash
$ git status
HEAD detached at dd3f28a

$ cat hello.txt
aaa

$ git log --oneline
dd3f28a (HEAD) A

$ git log --all --oneline
07af7f6 (main) C
3a2d414 B
dd3f28a (HEAD) A
```

### 다시 최신으로 복귀

```bash
$ git checkout main
Previous HEAD position was dd3f28a A
Switched to branch 'main'

$ cat hello.txt
aaa
bbb
ccc
```

***

## 1️⃣1️⃣ 전체 시간 여행 시나리오

### 완전한 실습 흐름

```bash
# 1. 현재 상태 확인
$ git log --oneline
07af7f6 (HEAD -> main) C
3a2d414 B
dd3f28a A

# 2. 작업 영역 정리 (stash 사용)
$ git stash

# 3. 이전 버전으로 이동 (B)
$ git checkout HEAD~
$ cat hello.txt
aaa
bbb

# 4. 로그 확인
$ git log --oneline --all
07af7f6 (main) C
3a2d414 (HEAD) B
dd3f28a A

# 5. 더 과거로 이동 (A)
$ git checkout HEAD~
$ cat hello.txt
aaa

# 6. 최신으로 복귀
$ git checkout main
$ cat hello.txt
aaa
bbb
ccc

# 7. stash 복구 (선택사항)
$ git stash pop
```

***

## 1️⃣2️⃣ git checkout vs git switch

### 명령어 비교표

| 설명 | git checkout | git switch |
|------|-------------|------------|
| **이전 커밋으로 이동** | `$ git checkout [이전커밋]` | `$ git switch -d [이전커밋]` |
| **다른 브랜치로 이동** | `$ git checkout [branch]` | `$ git switch [branch]` |
| **새 브랜치 생성 및 이동** | `$ git checkout -b [newBranch]` | `$ git switch -c [newBranch]` |

### git switch 사용 예시

```bash
# 과거 커밋으로 이동 (detach 옵션 필요)
$ git switch -d HEAD~
$ git switch -d dd3f28a

# 브랜치로 이동
$ git switch main

# 이전 위치로
$ git switch -

# 새 브랜치 생성 및 이동
$ git switch -c feature-branch
```

⭐ **추천**: Git 2.23 이상에서는 `git switch` 사용 권장

***

## 📌 핵심 요약 (Summary)

### 저장소 생성
```bash
$ git init basic
```

### Git 저장소에 저장
```bash
$ git add .
$ git commit -m "메시지"
```

### 모든 커밋 이력 보기
```bash
$ git log
$ git log --oneline --graph --all -n
```

**주요 옵션**:
- `--oneline`: 한 줄로 간결하게
- `--graph`: 그래프 형태
- `--all`: 모든 브랜치
- `-n`: 최근 n개

### 특정 커밋 이력 보기
```bash
$ git show [HEAD]
$ git show --oneline
```

### 바로 이전 버전으로 가기
```bash
$ git checkout HEAD~
```

⚠️ **주의**: 작업 영역이 깨끗해야 함 (`git stash` 또는 `git restore` 사용)

### 다시 최신 버전으로 돌아오기
```bash
$ git checkout main
```

### 다시 checkout 이전으로 돌아오기
```bash
$ git checkout -
```

***

## 실습 체크리스트

### 필수 실습 항목

- [ ] Git 전역 설정 완료
- [ ] `git init basic`으로 저장소 생성
- [ ] 3번의 커밋 생성 (A, B, C)
- [ ] `git log --oneline --graph --all` 실행
- [ ] `git show HEAD`, `HEAD~`, `HEAD~2` 확인
- [ ] `git stash`로 작업 영역 정리
- [ ] `git checkout HEAD~`로 과거 이동
- [ ] `cat hello.txt`로 과거 파일 내용 확인
- [ ] `git log --all --oneline`으로 전체 이력 확인
- [ ] `git checkout main`으로 최신 버전 복귀
- [ ] `git checkout -`로 직전 위치 이동 테스트