# 파일 비교 diff (Git File Comparison)

## 📋 학습 개요

### 학습 내용
- Git 3영역의 파일 비교
  - 작업 디렉토리 (Working Directory)
  - 스테이징 영역 (Staging Area)
  - Git 저장소 (Repository)
- 커밋 간의 파일 비교
  - HEAD (최신 커밋을 가리키는 포인터)

### 학습 목표
- 파일 비교 표시를 이해할 수 있다
- Git 3영역의 파일을 비교할 수 있다
  - 스테이징 영역 기준으로 작업 디렉토리 파일 비교
  - Git 저장소 기준으로 스테이징 영역 파일 비교
  - Git 저장소 기준으로 작업 디렉토리 파일 비교
- 커밋 간의 파일을 비교할 수 있다

***

## 1️⃣ 파일 비교 개요

### 비교 대상

#### Git 3영역의 파일 비교
- Working Directory ↔ Staging Area
- Staging Area ↔ Repository
- Working Directory ↔ Repository

#### 커밋 간의 파일 비교
- `HEAD`: 최신 커밋
- `HEAD~` 또는 `HEAD^`: 하나 전 커밋 (tilde: 틸드, 물결 / caret: 캐럿, 모자, 삿갓)
- `HEAD~~`, `HEAD~2`, `HEAD^^`, `HEAD^~`: 두 개 이전 커밋

#### 브랜치 간의 파일 비교
- 서로 다른 브랜치의 파일 비교

***

## 2️⃣ Git 3영역 구조

### 3영역의 이해

```
┌────────────────────────────────────────────┐
│         탐색기 저장소 폴더                  │
│                                            │
│  Working Directory                         │
│  (작업 디렉토리)                           │
│                                            │
│  - 리눅스 명령어 ls로 보이는 파일          │
│  - PC 탐색기에서 보이는 파일               │
└──────────────┬─────────────────────────────┘
               │ git add
               ↓
┌────────────────────────────────────────────┐
│         저장소 .git 폴더                    │
│                                            │
│  Staging Area (Index)                      │
│  (스테이징 영역)                           │
│                                            │
│  - git ls-files로 보이는 파일              │
│  - .git 폴더에 정보로 숨겨진 파일          │
└──────────────┬─────────────────────────────┘
               │ git commit
               ↓
┌────────────────────────────────────────────┐
│         저장소 .git 폴더                    │
│                                            │
│  Git Local Repository                      │
│  (Git 지역 저장소)                         │
│                                            │
│  - .git 폴더에 정보로 숨겨진 파일          │
└────────────────────────────────────────────┘
```

### 각 영역의 별칭

#### Working Directory
- Working Folder
- Working Tree
- Work Space
- Working Area

#### Staging Area
- Stage Area
- Index

#### Repository
- Git Repository
- Git Local Repository
- Last Commit (HEAD)

***

## 3️⃣ Git 3영역 파일 비교 명령어

### 비교 기준 개념

⭐ **핵심**: 두 영역을 비교할 때 **오른쪽 영역이 기준**이다[1]

```
Working Directory → Staging Area → Repository
   (비교 대상)        (기준)         (기준)
```

### 3가지 핵심 비교 명령어

```
Working Directory ← git diff → Staging Area

Staging Area ← git diff --staged → Repository

Working Directory ← git diff HEAD → Repository
```

#### 명령어 요약

| 명령어 | 비교 대상 | 기준 | 설명 |
|--------|----------|------|------|
| `$ git diff` | Working Directory | Staging Area | 스테이징 영역 기준으로 작업 디렉토리 비교 |
| `$ git diff --staged` | Staging Area | Repository (HEAD) | 저장소 기준으로 스테이징 영역 비교 |
| `$ git diff HEAD` | Working Directory | Repository (HEAD) | 저장소 기준으로 작업 디렉토리 비교 |

***

## 4️⃣ git diff (스테이징 기준 작업 디렉토리 비교)

### 명령어

```bash
$ git diff
```

### 비교 구조

```
Working Directory  ← 비교 →  Staging Area
  (비교 대상)                   (기준)
     b/                          a/
```

### 실습 예제

#### 상황 설정

```
Working Directory     Staging Area        Repository
hello.txt             hello.txt           hello.txt
create                create              create
add commit            add commit          add commit
update ...            update ...          update ...
diff                  (없음)              (없음)
```

#### 명령 실행

```bash
$ git diff
```

#### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index abc7307..c192580 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1,3 +1,4 @@
 create
 add commit
 update at working dir
+diff
```

### 출력 결과 해석

#### 헤더 정보

```diff
diff --git a/hello.txt b/hello.txt
```
- **비교 기준 파일** (Staging Area): `a/hello.txt`
- **비교 대상 파일** (Working Directory): `b/hello.txt`

#### 파일 해시
```diff
index abc7307..c192580 100644
```
- `abc7307`: Staging Area 파일의 해시
- `c192580`: Working Directory 파일의 해시
- `100644`: 파일 권한 (일반 파일)

#### 파일 표기
```diff
--- a/hello.txt
+++ b/hello.txt
```
- `---`: 기준 파일 (`a/hello.txt`)
- `+++`: 대상 파일 (`b/hello.txt`)

#### 변경 범위
```diff
@@ -1,3 +1,4 @@
```
- `-1,3`: 기준 파일은 **첫 번째 줄에서 3개의 줄** 비교
- `+1,4`: 대상 파일은 **첫 번째 줄에서 4개의 줄** 비교

#### 내용 비교
```diff
 create
 add commit
 update at working dir
+diff
```
- 공백 (` `): 양쪽에서 공통인 내용
- `+diff`: 대상 파일에 추가된 줄 (네 번째 줄)

### diff 기호 의미

| 기호 | 의미 | 설명 |
|------|------|------|
| ` ` (공백) | 공통 | 양쪽 파일에 모두 존재하는 줄 |
| `+` | 추가 | 대상 파일(b/)에만 존재하는 줄 |
| `-` | 삭제 | 기준 파일(a/)에만 존재하는 줄 |

***

## 5️⃣ git diff --staged (저장소 기준 스테이징 비교)

### 명령어

```bash
$ git diff --staged [HEAD]
$ git diff --cached [HEAD]
```

⭐ **참고**: `--staged`와 `--cached`는 동일한 기능

### 비교 구조

```
Staging Area  ← 비교 →  Repository (HEAD)
 (비교 대상)                (기준)
     b/                      a/
```

### 실습 예제

#### 상황 설정

```
Working Directory     Staging Area        Repository
hello.txt             hello.txt           hello.txt
create                create              create
add commit            add commit          add commit
update ...            update ...          update ...
diff                  (없음)              (없음)
```

**현재 상태**: Staging Area와 Repository가 동일

#### 명령 실행

```bash
$ git diff --staged
$ git diff --cached
```

#### 출력 결과

```
(결과 없음 - 비교 파일이 동일하므로)
```

⭐ **핵심**: **양쪽이 같으면 결과가 표시되지 않음**

### 차이가 있는 경우

#### 상황: 파일 수정 후 스테이징

```bash
$ echo "new line" >> hello.txt
$ git add hello.txt
```

```
Staging Area          Repository
hello.txt             hello.txt
create                create
add commit            add commit
update ...            update ...
new line              (없음)
```

#### 명령 실행

```bash
$ git diff --staged
```

#### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index abc7307..d8e9f10 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1,3 +1,4 @@
 create
 add commit
 update at working dir
+new line
```

***

## 6️⃣ git diff HEAD (저장소 기준 작업 디렉토리 비교)

### 명령어

```bash
$ git diff HEAD
```

### 비교 구조

```
Working Directory  ← 비교 →  Repository (HEAD)
   (비교 대상)                  (기준)
        b/                       a/
```

⭐ **특징**: Staging Area를 **건너뛰고** 직접 비교

### 실습 예제

#### 상황 설정

```
Working Directory     Staging Area        Repository
hello.txt             hello.txt           hello.txt
create                create              create
add commit            add commit          add commit
update ...            update ...          update ...
diff                  (없음)              (없음)
```

#### 명령 실행

```bash
$ git diff HEAD
```

#### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index abc7307..c192580 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1,3 +1,4 @@
 create
 add commit
 update at working dir
+diff
```

### 출력 결과 해석

```diff
@@ -1,3 +1,4 @@
```
- `-1,3`: 기준 파일(Repository)은 **첫 번째 줄에서 3개의 줄** 비교
- `+1,4`: 대상 파일(Working Directory)은 **첫 번째 줄에서 4개의 줄** 비교

```diff
 create
 add commit
 update at working dir
+diff
```
- 공백: 양쪽에서 공통인 내용
- `+diff`: 대상 파일(Working Directory)의 네 번째 줄에 추가된 내용

***

## 7️⃣ git diff HEAD~N (과거 커밋과 비교)

### 명령어

```bash
$ git diff HEAD~
$ git diff HEAD~2
$ git diff HEAD^^
```

### 실습: HEAD~2와 비교

#### 상황 설정

```
A (HEAD~2)            B (HEAD~)             C (HEAD)
hello.txt             hello.txt             hello.txt
create                create                create
                      add commit            add commit
                                            update
```

```
Working Directory
hello.txt
create
add commit
update
diff
```

#### 명령 실행

```bash
$ git diff HEAD~2
```

#### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index abc7307..c192580 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1 +1,4 @@
 create
+add commit
+update at working dir
+diff
```

### 출력 결과 해석

```diff
@@ -1 +1,4 @@
```
- `-1`: 기준 파일(HEAD~2)은 **1개의 줄**
- `+1,4`: 대상 파일(Working Directory)은 **첫 번째 줄에서 4개의 줄**

```diff
 create
+add commit
+update at working dir
+diff
```
- `create`: 공통 내용
- `+add commit`, `+update at working dir`, `+diff`: 대상 파일에 추가된 3개 줄

***

## 8️⃣ 두 커밋 간 비교

### 명령어 형식

```bash
$ git diff <commit-id-1> <commit-id-2>
$ git diff HEAD~2 HEAD~
$ git diff HEAD~ HEAD
```

### 비교 원리

```
커밋 A  ← 비교 →  커밋 B
 (기준)           (대상)
  a/               b/
```

⭐ **핵심**: 왼쪽 커밋이 기준(`a/`), 오른쪽 커밋이 대상(`b/`)

***

## 9️⃣ 실습 1: HEAD~2와 HEAD~ 비교

### 커밋 이력

```
A (HEAD~2)            B (HEAD~)             C (HEAD)
dd3f28a               3a2d414               07af7f6
hello.txt             hello.txt             hello.txt
create                create                create
                      add commit            add commit
                                            update
```

### 명령 실행

```bash
$ git diff HEAD~2 HEAD~
```

### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index 359af40..6ad3d3b 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1 +1,2 @@
 create
+add commit
```

### 해석
- A 커밋: `create` (1줄)
- B 커밋: `create` + `add commit` (2줄)
- **차이**: `add commit` 줄이 추가됨

***

## 🔟 실습 2: HEAD~와 HEAD 비교

### 명령 실행

```bash
$ git diff HEAD~ HEAD
```

### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index 6ad3d3b..abc7307 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1,2 +1,3 @@
 create
 add commit
+update at working dir
```

### 해석
- B 커밋: `create` + `add commit` (2줄)
- C 커밋: `create` + `add commit` + `update at working dir` (3줄)
- **차이**: `update at working dir` 줄이 추가됨

***

## 1️⃣1️⃣ 실습 3: HEAD~2와 HEAD 비교

### 명령 실행 (2가지 방법)

```bash
# 방법 1: HEAD 표기 사용
$ git diff HEAD~2 HEAD

# 방법 2: 커밋 해시 직접 사용
$ git diff dd3f28a 07af7f6
```

### 출력 결과

```diff
diff --git a/hello.txt b/hello.txt
index 395af40..abc7307 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1 +1,3 @@
 create
+add commit
+update at working dir
```

### 해석
- A 커밋: `create` (1줄)
- C 커밋: `create` + `add commit` + `update at working dir` (3줄)
- **차이**: 2개 줄이 추가됨

***

## 1️⃣2️⃣ diff 출력 형식 정리

### 기본 구조

```diff
diff --git a/<기준파일> b/<대상파일>
index <기준해시>..<대상해시> <권한>
--- a/<기준파일>
+++ b/<대상파일>
@@ -<기준시작>,<기준줄수> +<대상시작>,<대상줄수> @@
 <공통줄>
-<기준파일에만 있는 줄>
+<대상파일에만 있는 줄>
```

### 예제 분석

```diff
diff --git a/hello.txt b/hello.txt
index abc7307..c192580 100644
--- a/hello.txt
+++ b/hello.txt
@@ -1,3 +1,4 @@
 create
 add commit
 update at working dir
+diff
```

| 항목 | 내용 | 설명 |
|------|------|------|
| `a/hello.txt` | 기준 파일 | Staging Area 또는 Repository |
| `b/hello.txt` | 대상 파일 | Working Directory 또는 다른 커밋 |
| `---` | 기준 파일 표기 | `-`로 시작하는 줄은 여기서 삭제됨 |
| `+++` | 대상 파일 표기 | `+`로 시작하는 줄은 여기에 추가됨 |
| `@@` | 변경 범위 | `-1,3`: 기준 1번줄부터 3줄, `+1,4`: 대상 1번줄부터 4줄 |
| ` ` (공백) | 공통 줄 | 변경 없음 |
| `+` | 추가 줄 | 대상 파일에만 존재 |
| `-` | 삭제 줄 | 기준 파일에만 존재 |

***

## 📌 핵심 요약 (Summary)

### 스테이징 영역 기준으로 작업 디렉토리 파일 비교
```bash
$ git diff
```
- Working Directory와 Staging Area 비교
- 가장 자주 사용하는 명령어

### Git 저장소 기준으로 스테이징 영역 파일 비교
```bash
$ git diff --staged HEAD
$ git diff --cached HEAD
$ git diff --staged HEAD~
```
- Staging Area와 Repository 비교
- 커밋 전 확인용

### Git 저장소 기준으로 작업 디렉토리 파일 비교
```bash
$ git diff HEAD
$ git diff HEAD~
$ git diff HEAD~2
```
- Working Directory와 Repository 비교
- Staging 단계를 건너뛴 비교

### 커밋 간의 파일 비교
```bash
$ git diff <commit-hash-1> <commit-hash-2>
$ git diff 048171 0c747d
$ git diff HEAD^ HEAD
$ git diff HEAD~2 HEAD
```
- 두 커밋 간 차이 확인
- 버전 변화 추적

***

## 실습 체크리스트

### 필수 실습 항목

- [ ] `git diff` 실행하여 Working Directory와 Staging Area 차이 확인
- [ ] `git diff --staged` 실행하여 Staging Area와 Repository 차이 확인
- [ ] `git diff HEAD` 실행하여 Working Directory와 Repository 차이 확인
- [ ] `git diff HEAD~` 실행하여 과거 커밋과 비교
- [ ] `git diff HEAD~2 HEAD` 실행하여 두 커밋 비교
- [ ] diff 출력에서 `@@`, `+`, `-` 기호 의미 이해
- [ ] `a/` 경로와 `b/` 경로의 차이 이해