# 파일 삭제 rm과 복원 restore (Git File Deletion & Restoration)

## 📋 학습 개요

### 학습 내용
1. `$ rm f` - 리눅스 파일 삭제
2. `$ git rm f` - Git 파일 삭제
3. `$ git rm --cached f` - 스테이징 영역 파일 삭제
4. `$ git restore f` - 파일 복원
5. `$ git restore --staged f` - 스테이징 취소

### 학습 목표
1. Git의 3영역 중에서 작업 디렉토리와 스테이징 영역에서 파일을 삭제할 수 있다
2. 특정 파일에서 스테이징 영역의 상태를 작업 디렉토리에 복원할 수 있다
3. 특정 파일에서 Git 저장소 상태를 스테이징 영역에 복원할 수 있다
4. 특정 파일에서 Git 저장소 상태를 작업 디렉토리에 복원할 수 있다
5. 특정 파일에서 Git 저장소 상태를 스테이징 영역과 작업 디렉토리에 함께 복원할 수 있다

***

## 1️⃣ 파일 삭제 명령어 개요

### 삭제 명령어 비교

| 명령어 | 작업 디렉토리 | 스테이징 영역 | 설명 |
|--------|-------------|-------------|------|
| `$ rm [file]` | ❌ 삭제 | ⭕ 유지 | 리눅스 명령어 - 작업 디렉토리에서만 삭제 |
| `$ git rm [file]` | ❌ 삭제 | ❌ 삭제 | Git 명령어 - 두 영역에서 모두 삭제 |
| `$ git rm --cached [file]` | ⭕ 유지 | ❌ 삭제 | Git 명령어 - 스테이징 영역에서만 삭제 |

### 명령어 설명

#### 1. rm (리눅스 명령)
```bash
$ rm [file]
```
- **작업 디렉토리에서만 file 삭제**
- 스테이징 영역과 저장소는 영향 없음
- 일반적인 파일 시스템 삭제

#### 2. git rm (Git 명령)
```bash
$ git rm [file]
```
- **작업 디렉토리와 스테이징 영역에서 모두 file 삭제**
- 다음 커밋에서 지정한 file을 삭제하겠다는 의미
- Tracked 상태의 파일을 제거하여 Untracked 상태로 만듦

#### 3. git rm --cached (Git 명령)
```bash
$ git rm --cached [file]
```
- **스테이징 영역에서만 file 삭제**
- 작업 디렉토리에서는 삭제되지 않음
- `$ git ls-files` 결과에서 보이지 않음 (기본적으로 스테이징 영역의 파일 목록 표시)

***

## 2️⃣ git rm - 작업 디렉토리와 스테이징 영역에서 파일 삭제

### 초기 상태

```
Working Directory     Staging Area      Repository
      f                     f                 f
      g                     g                 g
```

3영역(WD, SA, GR) 모두에 파일 f, g가 있는 상태

### 명령 실행

```bash
$ git rm g
```

### 실행 후 상태

```
Working Directory     Staging Area      Repository
      f                     f                 f
     (삭제)                (삭제)              g
```

### 상태 확인

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ ls
f

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git ls-files
f

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git status
On branch main
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        deleted:    g

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git status -s
D  g
```

⭐ **중요**: 녹색 `D`는 스테이징 영역에서 삭제 예정을 의미 (Git 저장소와 비교해 삭제됨)

### 커밋 후 최종 상태

```bash
$ git commit -m 'Delete g'
[main 49b0613] Delete g
 1 file changed, 0 insertions(+), 0 deletions(-)
 delete mode 100644 g
```

#### git show 확인

```bash
$ git show
commit 49b0613f77ea47f169ed01b56af5e92b0d7f7dbb (HEAD -> main)
Author: ai7dnn <ai7dnn@gmail.com>
Date:   Sat Jan 21 15:57:23 2023 +0900

    Delete g

diff --git a/g b/g
deleted file mode 100644
index e69de29..0000000
```

### 최종 상태

```
Working Directory     Staging Area      Repository
      f                     f                 f
     (없음)                (없음)            (없음)
```

파일 g가 3영역 모두에서 삭제됨

***

## 3️⃣ git rm --cached - 스테이징 영역에서만 파일 삭제

### 초기 상태

```
Working Directory     Staging Area      Repository
      f                     f                 f
      g                     g                 g
```

### 명령 실행

```bash
$ git rm --cached f
```

### 실행 후 상태

```
Working Directory     Staging Area      Repository
      f                  (삭제)               f
      g                     g                 g
```

### 상태 확인

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ ls
f

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git ls-files
(빈 결과 또는 g만 표시)

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git status
On branch main
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        deleted:    f
        deleted:    g

Untracked files:
  (use "git add <file>..." to include in what will be committed)
        f

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git status -s
D  f
D  g
?? f
```

⭐ **핵심**: `??`은 Untracked를 의미하며, 스테이징 영역에서 파일이 삭제되어 추적되지 않는 파일이 된 상태

***

## 4️⃣ rm - 작업 디렉토리에서만 파일 삭제

### 초기 상태

```
Working Directory     Staging Area      Repository
      f                  (없음)               f
      g                     g                 g
```

### 명령 실행

```bash
$ rm f
```

### 실행 후 상태

```
Working Directory     Staging Area      Repository
     (삭제)              (없음)               f
      g                     g                 g
```

### 상태 확인

```bash
PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ ls
(빈 결과 또는 다른 파일만 표시)

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git ls-files
(빈 결과)

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git status
On branch main
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        deleted:    f
        deleted:    g

PC@DESKTOP-482NOAB MINGW64 /c/[smart Git]/rm (main)
$ git status -s
D  f
D  g
```

***

## 5️⃣ 파일 복원 restore 개요

### 복원 방향

```
┌─────────────────┐     restore      ┌─────────────────┐
│   Staging Area  │ ───────────────→ │Working Directory│
│                 │                   │                 │
└─────────────────┘                   └─────────────────┘
        ↑                                      ↑
        │                                      │
        │ restore --staged                     │
        │                                      │ restore --source=HEAD
        │                                      │
┌─────────────────────────────────────────────┘
│   Git Repository                             
│   (HEAD)                                     
└──────────────────────────────────────────────
```

### 복원 명령어 요약

| 명령어 | 복원 방향 | 설명 |
|--------|----------|------|
| `$ git restore f` | Staging → Working | 스테이징 영역 → 작업 디렉토리 |
| `$ git restore --staged f` | Repository → Staging | 저장소 → 스테이징 영역 |
| `$ git restore --source=HEAD f` | Repository → Working | 저장소 → 작업 디렉토리 |
| `$ git restore --source=HEAD --staged --worktree f` | Repository → Both | 저장소 → 스테이징+작업 디렉토리 |

***

## 6️⃣ 복원 실습 준비

### 3영역이 모두 다른 상태 만들기

#### Step 1: 초기 커밋

```bash
$ git add f
$ git commit -m "Initial commit"
```

```
Working Directory     Staging Area      Repository
      f                     f                 f
```

#### Step 2: 파일 수정 후 add

```bash
$ echo "update 1" >> f
$ git add f
```

```
Working Directory     Staging Area      Repository
   f (update 1)        f (update 1)         f
```

#### Step 3: 다시 파일 수정

```bash
$ echo "update 2" >> f
```

```
Working Directory       Staging Area        Repository
 f (update 1, 2)        f (update 1)            f
```

⭐ **현재 상태**: 3영역에서 파일 f가 모두 다른 상태

***

## 7️⃣ git restore - 스테이징 영역 → 작업 디렉토리

### 명령어

```bash
$ git restore [file]
```

### 초기 상태

```
Working Directory       Staging Area        Repository
 f (update 1, 2)        f (update 1)            f
```

### 명령 실행

```bash
$ git restore f
```

### 실행 후 상태

```
Working Directory       Staging Area        Repository
   f (update 1)         f (update 1)            f
```

⚠️ **주의**: 작업 디렉토리에 있던 f 내용(update 2)이 **사라지므로 유의**

### 결과
- 3영역에서 파일 f가 작업 디렉토리와 스테이징 영역이 **같은 상태**가 됨

***

## 8️⃣ git restore --staged - 저장소 → 스테이징 영역

### 명령어

```bash
$ git restore --staged [file]
```

### 초기 상태

```
Working Directory       Staging Area        Repository
 f (update 1, 2)        f (update 1)            f
```

### 명령 실행

```bash
$ git restore --staged f
```

### 실행 후 상태

```
Working Directory       Staging Area        Repository
 f (update 1, 2)            f                   f
```

⚠️ **주의**: 스테이징 영역에 있던 f 내용(update 1)이 **사라지므로 유의**

### 결과
- 3영역에서 **스테이징 영역의 파일 f가 Git 저장소 영역과 같은 상태**가 됨
- 이전에 add한 내용이 취소됨 (Unstage)

***

## 9️⃣ git restore --source=HEAD - 저장소 → 작업 디렉토리

### 명령어

```bash
$ git restore --source=HEAD [file]
$ git restore --source=HEAD --worktree [file]
```

⭐ **참고**: `--worktree`는 기본값이므로 생략 가능

### 초기 상태

```
Working Directory       Staging Area        Repository
 f (update 1, 2)        f (update 1)            f
```

### 명령 실행

```bash
$ git restore --source=HEAD f
```

### 실행 후 상태

```
Working Directory       Staging Area        Repository
        f               f (update 1)            f
```

### 결과
- 파일 f가 **현재 커밋(HEAD) 상태의 내용**으로 작업 디렉토리에 복사되어 동일하게 됨
- 스테이징 영역은 변경 없음

***

## 🔟 git restore (한 번에 모두 복원)

### 명령어

```bash
$ git restore --source=HEAD --staged --worktree [file]
```

### 초기 상태

```
Working Directory       Staging Area        Repository
 f (update 1, 2)        f (update 1)            f
```

### 명령 실행

```bash
$ git restore --source=HEAD --staged --worktree f
```

### 실행 후 상태

```
Working Directory       Staging Area        Repository
        f                     f                   f
```

### 결과
- Git 저장소의 **최신 커밋 상태의 파일 f를 작업 디렉토리와 스테이징 영역에 한 번에 복구**
- 파일 f가 현재 커밋 상태의 내용으로 작업 디렉토리와 스테이징 영역 **모두 같은 상태**가 됨

⭐ **핵심**: 3영역을 완전히 동일하게 만드는 명령어

***

## 1️⃣1️⃣ git restore --source=HEAD^ (과거 커밋으로 복원)

### 명령어

```bash
$ git restore --source=HEAD^ --staged [file]
$ git restore --source=HEAD~ --staged [file]
```

### 의미
- `--source=HEAD^`: **복구에 사용되는 파일이 위치한 커밋을 지정**
- `HEAD~` 또는 `HEAD^`: 이전 커밋의 내용으로 복구한다는 의미

### 초기 상태

```
Working Directory       Staging Area        Repository (HEAD)
 f (update 1, 2)        f (update 1)            f (current)
```

### 명령 실행

```bash
$ git restore --source=HEAD~ --staged f
```

### 실행 후 상태

```
Working Directory       Staging Area        Repository
 f (update 1, 2)        f (previous)            f (current)
```

### 결과
- 스테이징 영역이 **이전 커밋(HEAD~)의 상태**로 복원됨

***

## 1️⃣2️⃣ restore 명령어 완전 정리

### 복원 흐름도

```
┌──────────────────────────────────────────────────┐
│          Working Directory                        │
│          (작업 디렉토리)                          │
└────────────┬─────────────────────────────────────┘
             │ ↑
             │ │ git restore
             │ │
             ↓ │
┌──────────────────────────────────────────────────┐
│          Staging Area                             │
│          (스테이징 영역)                          │
└────────────┬─────────────────────────────────────┘
             │ ↑
             │ │ git restore --staged
             │ │
             ↓ │
┌──────────────────────────────────────────────────┐
│          Git Repository                           │
│          (Git 저장소)                             │
└──────────────────────────────────────────────────┘
```

### 주요 명령어

#### 1. 스테이징 → 작업 디렉토리
```bash
$ git restore f
```
- 스테이징 영역의 상태를 작업 디렉토리에 복원

#### 2. 저장소 → 스테이징
```bash
$ git restore --staged f
```
- Git 저장소(HEAD) 상태를 스테이징 영역에 복원
- **Unstage** (add 취소)

#### 3. 저장소 → 작업 디렉토리
```bash
$ git restore --source=HEAD f
$ git restore --source=HEAD --worktree f
```
- Git 저장소 상태를 작업 디렉토리에 복원

#### 4. 저장소 → 스테이징 + 작업 디렉토리
```bash
$ git restore --source=HEAD --staged --worktree f
```
- Git 저장소 상태를 스테이징 영역과 작업 디렉토리에 함께 복원

### 옵션 축약

⭐ **축약형**:
- `--staged` → `-S` (대문자)
- `--worktree` → `-W` (대문자)

```bash
# 축약 사용 예
$ git restore -S f
$ git restore --source=HEAD -W f
$ git restore --source=HEAD -S -W f
```

***

## 📌 핵심 요약 (Summary)

### 작업 디렉토리에서 파일 삭제
```bash
$ rm f
```
- 리눅스 명령어
- 작업 디렉토리에서만 삭제

### 작업 디렉토리와 스테이징 영역에서 파일 삭제
```bash
$ git rm f
```
- Git 명령어
- 두 영역에서 모두 삭제
- 다음 커밋에 삭제가 반영됨

### 스테이징 영역에서만 파일 삭제
```bash
$ git rm --cached f
```
- Git 명령어
- 스테이징 영역에서만 삭제
- 작업 디렉토리 파일은 유지 (Untracked 상태로 변경)

### 스테이징 영역의 상태를 작업 디렉토리에 복원
```bash
$ git restore f
```
- 스테이징 → 작업 디렉토리
- 작업 디렉토리의 변경사항 취소

### Git 저장소 상태를 스테이징 영역에 복원
```bash
$ git restore --staged f
$ git restore -S f
```
- 저장소 → 스테이징
- Unstage (add 취소)

### Git 저장소 상태를 작업 디렉토리에 복원
```bash
$ git restore --source=HEAD --worktree f
$ git restore --source=HEAD f
$ git restore --source=HEAD -W f
```
- 저장소 → 작업 디렉토리

### Git 저장소 상태를 스테이징 영역과 작업 디렉토리에 함께 복원
```bash
$ git restore --source=HEAD --staged --worktree f
$ git restore --source=HEAD -S -W f
```
- 저장소 → 스테이징 + 작업 디렉토리
- 3영역 완전 동기화

***

## 🎯 실습 체크리스트

### 파일 삭제 실습

- [ ] `rm f` 실행 후 `ls`, `git ls-files`, `git status` 확인
- [ ] `git rm g` 실행 후 상태 확인 및 커밋
- [ ] `git rm --cached f` 실행 후 Untracked 상태 확인

### 파일 복원 실습

- [ ] 3영역이 모두 다른 상태 만들기 (수정 → add → 수정)
- [ ] `git restore f` 실행하여 작업 디렉토리 복원
- [ ] `git restore --staged f` 실행하여 스테이징 취소
- [ ] `git restore --source=HEAD f` 실행하여 작업 디렉토리 복원
- [ ] `git restore --source=HEAD -S -W f` 실행하여 완전 복원
- [ ] `git diff`, `git diff --staged` 사용하여 각 단계 확인
